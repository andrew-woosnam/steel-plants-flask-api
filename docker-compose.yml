version: "3.8"

services:
  db:
    image: andrewtwoosnam/psql-iron-steel-plants
    ports:
      - "5432:5432"
    env_file:
      - .env

  flask-api:
    build: .
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - .:/code
    ports:
      - "5000:5000"

  test:
    build:
      dockerfile: Dockerfile.test
    depends_on:
      - flask-api
    env_file:
      - .env
    command: pytest tests/
    profiles: [test]
